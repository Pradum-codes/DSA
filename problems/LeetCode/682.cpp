#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int calPoints(vector<string>& operations) {
        
    }
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n; cin >> n;
    vector<string> oprn(n);

    for(int i = 0; i<n; i++) cin >> oprn[i];
    
    Solution sol;
    sol.calPoints(oprn);
    return 0;
}